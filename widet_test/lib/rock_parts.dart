import 'package:flutter/material.dart';

class RockParts extends StatelessWidget {
  const RockParts({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Text("部品っちゃね"),
    );
  }
}
